[build]
  cmd = "go build -o ./tmp/main ./cmd/api/"
  bin = "./tmp/main"
  delay = 1000
  exclude_dir = ["tmp", "vendor", "keys"]
  include_ext = ["go"]
  kill_delay = "0s"

[log]
  time = false

[misc]
  clean_on_exit = true
